# Leave empty for local dev: browser calls /api on Vite (:5173); Vite proxies to mams-server (:3001).
# Set a full URL only if you need a remote API (e.g. production build against a staging host).
VITE_API_BASE_URL=
VITE_DEMO_MODE=true
